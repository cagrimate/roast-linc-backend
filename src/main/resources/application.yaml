spring:
  application:
    name: linc

  datasource:
    url: jdbc:postgresql://localhost:5432/court_db
    username: postgres
    password: postgres123
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.PostgreSQLDialect
  web:
    error:
      include-message: always

server:
  port: 8080

springdoc:
  api-docs:
    path: /v3/api-docs
  swagger-ui:
    path: /swagger-ui.html
    operationsSorter: alpha

cloudinary:
  url: "cloudinary://224825724857548:i-vAtAYRfuC9se3KEwLivCKd9YE@dpveuqast"
  cloud-name: "dpveuqast"
  api-key: "224825724857548"
  api-secret: "i-vAtAYRfuC9se3KEwLivCKd9YE"
